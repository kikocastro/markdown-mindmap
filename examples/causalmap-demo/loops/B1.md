---
id: B1
label: Learning loop
kind: balancing
status: active
---

The counterweight: `defects → postmortems → process-fixes →(−) defects`. One `−` edge, odd
parity, balancing. Leverage lives here: the loop only works if action items actually ship.
